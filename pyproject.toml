[tool.ruff]
target-version = "py313"
line-length = 100
exclude = [
  ".venv",
  "data",
  "logs",
  "temp",
  "tests/output",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

# AGENTS.md

## 角色
繁體中文 Python 工程師。
修改程式前先說明要做什麼、怎麼拆分、怎麼驗證。

## 開發環境
- Windows 使用 PowerShell。
- 路徑由 `pathlib.Path` 處理。
- 專案必須使用 `.venv`。
- Python 檔案使用 UTF-8。

## 程式規範
- 每個 Python 檔案頂端加入 `# -*- coding: utf-8 -*-`。
- 常數使用全大寫並集中在 `config.py`。
- 函式只處理一件事，原則上控制在 20 行內。
- 類別只封裝相關功能，原則上控制在 30 行內。
- 函式上方使用繁體中文註解。
- 禁止 `import *`。
- 禁止寫死絕對路徑。
- 本機機密資料只放在 `.env`；GitHub Actions 機密資料只放在 repository Secrets。

## 公告資料品質規範
- 原始蒐集數、申請型公告數、非申請型公告數必須分開顯示。
- 來源網站數與跨來源去重後公告數不得混用。
- 獎學金、助學金、就學貸款、補助與其他公告必須分開分類。
- 就學貸款不得混入獎助金資格判斷或正式獎助通知。
- 申請型公告必須標示 open、upcoming、expired 或 deadline_unknown。
- 多來源報告必須顯示有資料、空結果、部分完成與失敗來源數。
- 完整稽核必須回報偵測頁數、成功頁數、原始列、解析列及排除列。
- 網站宣告多頁時，未抓完所有頁面只能標示 partial，不得標示 complete。
- 每日增量與完整稽核必須使用明確不同的 CollectionMode。
- 圓夢助學網的申請截止日不得寫入 published_date。
- 外部來源必須使用專用 collector，不得以單一通用 selector 假設網站結構一致。
- 新增或修正來源前必須人工確認入口頁、公告列表、年度明細與附件入口，不得只憑搜尋摘要或首頁名稱設定 URL。
- 官方來源、正式機構轉載與第三方彙整必須明確分級；正式轉載不得標成官方來源。
- 固定方案頁、年度單篇、公告列表與首頁必須使用對應的 SourceUrlType；年度單篇不得假裝可跨年度自動發現。
- 來源契約至少記錄主入口、fallback、合法 host、預期發現方式、最後驗證日期與跨年度風險。
- SSL 相容模式不得使用 verify=False、CERT_NONE 或關閉 hostname 驗證。

## 資格判斷規範
- 任一必要資格明確不符時立即 `ineligible`。
- 非申請公告不得列為個人資格不符。
- 已截止公告只代表目前不可申請，不得列為個人資格不符。
- 主要申請辦法、適用對象或必要條件未確認時維持 `review`。
- 個人資料未提供時維持 `review`，不得以 0、空字串或 false 冒充已確認事實。
- 必要條件與優先條件必須分開；優先條件不得造成 `ineligible`。
- `profile.json` 明確包含 `"special_statuses": []` 代表已確認沒有特殊或經濟弱勢身分；整個欄位缺失才代表未知。
- 低收入戶、清寒或經濟弱勢僅為優先、加分、同分優先時，一般學生仍保有申請資格。
- 只有公告明確寫明限、須具備、申請對象為或至少符合一項，且 profile 已確認沒有相關身分時，才能因弱勢條件判 `ineligible`。
- 「以弱勢為原則」「主要協助弱勢」等未說明一般生能否申請的文字必須維持 `review`。
- 同一句列出多個必要身分並以或、任一、擇一連接時，必須依 any-of 判斷，不得誤作 all-of。
- 跨行條列必須保留標題語意；「符合下列其中之一」是 any-of，「共同申請條件」或未寫任一的資格條列是 all-of。
- 頓號 `、` 只代表列舉，不能單獨作為 OR 的證據。
- 最近一學期、前一學年與累積成績必須使用各自欄位，不得混用。
- 成績、排名或領域單一正向條件不得單獨產生 `eligible`。
- 只有 `rules` 類附件可解除主要資格附件未知狀態。
- 表單、證明書、切結書或其他次要附件不得取代主要申請辦法。
- 資格規則語意變更時必須提升 `ELIGIBILITY_RULE_VERSION`，使未推播公告重新評估。

## 外部 AI 規範
- 本機規則優先，只有 `review` 的困難文件才能進入外部 AI。
- 掃描附件優先選擇 `rules` 類主要申請辦法，次要證明與表單不得消耗額度。
- 不得把 `profile.json`、LINE Token 或其他個資送往模型。
- 模型只負責結構化抽取，不得直接決定 `eligible`。
- 每次執行必須有呼叫數、頁數與 Token 上限。
- 相同文件必須依內容雜湊、模型與提示版本使用快取。
- 模型證據不足、頁面不完整或輸出驗證失敗時維持 `review`。
- Audit 必須顯示模型抽取欄位與頁碼證據，供人工核對。

## 雲端排程規範
- GitHub Actions 第一次正式使用前必須明確執行 `initialize` 建立歷史基準。
- 正式排程找不到既有雲端狀態時必須 fail closed，不得自動 baseline 或推播。
- SQLite 狀態上傳 artifact 前必須加密，解密密碼只放 GitHub Actions Secret。
- `profile.json` 只能由 Secret 在 runner 暫時建立，不得提交到 repository 或 artifact。
- 本機排程與雲端排程不得同時正式運作，避免兩份 SQLite 各自重複推播。
- Workflow 必須設定 concurrency，禁止正式流程重疊執行。

## 測試規範
- 修改既有邏輯前先執行 `pytest tests/`。
- 新專案先完成邏輯，再補獨立測試。
- 測試輸出放在 `tests/output/`。
- 所有測試通過後才能完成任務。

## 專案維護
- 規則變更時同步更新本檔。
- 不建立 `_copy`、`_backup` 或編號式版本檔案。
- 任務結束前清除暫存檔。
